repos:
- repo: local
  hooks:
    - id: precommit
      name: Run quick precommit checks
      entry: just precommit
      language: system
      always_run: true
      pass_filenames: false
      stages: [commit]
