version: "2"

run:
  modules-download-mode: readonly
  tests: false

linters:
  default: none
  enable:
    - revive
  settings:
    revive:
      rules:
        - name: exported
        - name: package-comments

  exclusions:
    generated: lax
